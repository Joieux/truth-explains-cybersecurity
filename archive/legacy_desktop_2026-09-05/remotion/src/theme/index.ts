export { colors } from "./colors";
export { fonts, fontWeights, fontSizes } from "./typography";
export { durations, easings } from "./motion";

// Video dimensions (1080p YouTube)
export const videoConfig = {
  width: 1920,
  height: 1080,
  fps: 30,
} as const;
