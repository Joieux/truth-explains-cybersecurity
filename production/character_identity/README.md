# Truth Explains Character Identity System

Rules and manifests in this directory are stored in Git.

Character reference images and production media are stored outside Git at:

`~/Documents/Codex/truth-explains-media/characters/`

Each character uses:

```text
characters/<character>/
├── identity/
│   ├── master/
│   └── expressions/
├── variants/
└── approved/
